# 7. 버그 추적 및 학습 기록

> AI가 과거 실수에서 학습하도록 만드는 '오류 기억 장치'. 같은 버그의 반복을 방지한다.

---

## 버그 수정 워크플로우

1. **브랜치 생성**: `fix/BUG-NNN` 브랜치를 만든다
2. **로그 확인**: `[ERROR]` 레벨 로그를 분석하여 원인을 파악
3. **재현 테스트**: 버그를 재현하는 테스트를 `5test.md` 회귀 섹션에 추가
4. **수정**: 최소 범위로 수정. 다른 파일은 건드리지 않는다
5. **전체 테스트**: 모든 테스트가 통과하는지 확인
6. **기록**: 이 파일에 버그를 기록
7. **커밋/머지**: fix 브랜치를 dev에 머지하고, 브랜치를 삭제

---

## 버그 기록

<!-- 아래 형식을 복사하여 새 버그를 기록한다 -->

<!--
## BUG-001: [버그 제목]
- **발생일**: YYYY-MM-DD
- **관련 로그**: `[ERROR] 로그에서 복사한 해당 로그 라인`
- **증상**: 어떤 현상이 발생했는가
- **근본 원인**: 왜 발생했는가 (5 Why 분석)
- **수정 내용**: 어떻게 고쳤는가
- **영향 범위**: 이 수정이 다른 모듈에 미치는 영향
- **재발 방지**: 이 유형의 버그를 막기 위한 규칙
- **추가된 테스트**: 5test.md 회귀 테스트 섹션에 추가한 테스트
- **Git**: fix/BUG-001 브랜치에서 수정, 커밋 해시 `abc1234`
-->

---

## 패턴 분석

> 같은 패턴의 버그가 3회 이상 반복되면, `2design.md`의 규칙으로 승격시킨다.

| 패턴 | 발생 횟수 | 관련 BUG ID | 승격 여부 |
|------|-----------|-------------|-----------|
| | | | ⬜ 미승격 / ✅ 승격 |

---

## 버그 수정 프롬프트

```
fix/BUG-NNN 브랜치를 만들고, 로그를 분석해서 원인을 찾아. 
재현 테스트 추가 → 수정 → 전체 테스트 → 7buglog.md 기록 → 커밋.
```
